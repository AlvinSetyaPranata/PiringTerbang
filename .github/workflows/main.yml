on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server:  ftp.byte.biz.id
        username: alvinsetyapranata@byte.biz.id
        password: testingthedatabases
        server-dir: "/home/pfulhebk/repositories/PiringTerbang/backend/"
        local-dir: "/media/fricheese/Local Disk/temp-projects/PiringTerbang/"
